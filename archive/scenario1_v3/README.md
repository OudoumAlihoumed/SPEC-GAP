# Archived Scenario 1 v3 draft

These files preserve an earlier Scenario 1 design for history only. They use
the superseded `spec_gap.scenario1.v3` format, Qwen2.5, hard-coded outputs, and
an older live-run path. They are not part of the active pipeline and should not
be used to generate experiment results.

Use the ordered commands under `scripts/` and the current v2 contract under
`schemas/scenario1/v2/` instead.
